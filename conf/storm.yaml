storm.zookeeper.servers:
  - "zookeeper"

nimbus.seeds: ["nimbus"]

# configure ports for duplicates (scaling)
# supervisor.slots.ports:
#   - 6700
#   - 6701
#   - 6702
#   - 6703

storm.local.dir: "/storm-data"
ui.port: 8080
